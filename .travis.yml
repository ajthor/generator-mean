language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mean' ]; then cd .. && eval "mv $currentfolder generator-mean" && cd generator-mean; fi

